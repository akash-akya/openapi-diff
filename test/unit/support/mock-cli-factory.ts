import {createMockFileSystem} from './mocks/mock-file-system';
import {createMockHttpClient} from './mocks/mock-http-client';

export const MockCliFactory = {
    createMockFileSystem,
    createMockHttpClient
};
